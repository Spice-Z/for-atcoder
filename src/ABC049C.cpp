#include <bits/stdc++.h>
using namespace std;

bool check(string &target)
{

  const vector<string> candidates = {"dreamer", "eraser", "dream", "erase"};
  for (int i = 0; candidates.size();)
  {
    // target切り取り
    candidates.at(i);

    // check(target)
  }

  if (target.size() == 0)
  {
    return true;
  }
  return false;
}

int main()
{
  string target;
  cout << target;
}